name: Deploy PharmaRAG

on:
  push:
    branches: [main]

concurrency:
  group: vps-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Upload sources
        run: |
          rsync -avz --exclude='node_modules' --exclude='.git' --exclude='dist' --exclude='.env' --exclude='__pycache__' \
            -e "ssh -i ~/.ssh/deploy_key" \
            . root@${{ secrets.VPS_HOST }}:/opt/pharma-rag/

      - name: Build and restart
        run: |
          ssh -i ~/.ssh/deploy_key root@${{ secrets.VPS_HOST }} << 'EOF'
          cd /opt/pharma-rag
          docker compose up -d --build --force-recreate frontend backend
          sleep 3
          curl -sf http://localhost:8000/api/health || echo "Health check failed"
          docker restart cheslav-nginx
          echo "Deploy complete"
          EOF

      - name: Notify Telegram
        if: success()
        run: |
          COMMITS=$(git log --oneline -5 --format="• %s" | head -5)
          MSG="💊 *PharmaRAG deployed*%0A%0A${COMMITS}%0A%0A🔗 [Open](https://cheslav.space/pharma-rag/)"
          curl -s "https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendMessage" \
            -d "chat_id=@pharma_rag_dev" \
            -d "text=$MSG" \
            -d "parse_mode=Markdown" \
            -d "disable_web_page_preview=true"
